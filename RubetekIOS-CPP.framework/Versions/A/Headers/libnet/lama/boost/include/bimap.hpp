#pragma once

#include <msw/config.hpp>

#ifdef MSW_MSVC
#   pragma warning (push)
#   pragma warning (disable: 4512)
#endif

#include <boost/bimap.hpp>

#ifdef MSW_MSVC
#   pragma warning (pop)
#endif
